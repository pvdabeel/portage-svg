digraph prolog {

# *************
# Graph options
# *************

newrank=true;
concentrate=true;
compound=true;
graph [rankdir=LR,fontname=Helvetica,fontsize=10,ranksep=1.5];#, ranksep=2.5, nodesep=0.2];
edge  [arrowhead=vee];
node  [fontname=Helvetica,fontsize=10];

# **********
# The ebuild
# **********

subgraph cluster_leftcol {
color=gray;
label=<<i>ebuild</i>>;
id [label="portage://net-p2p/bitcoind-25.1", color=red, width=4, href="../net-p2p/bitcoind-25.1.svg"];
}

# ****************
# The dependencies
# ****************

subgraph cluster_midcol {
color=gray;
label=<<i>dependencies</i>>;
subgraph cluster_compile {
fillcolor="#eeeeee";
style=filled;
label=<<i>compile</i>>;
}
subgraph cluster_compileandrun {
fillcolor="#eeeeee";
style=filled;
label=<<i>compile and run</i>>;
}
subgraph cluster_run {
fillcolor="#eeeeee";
style=filled;
label=<<i>run</i>>;
# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(use_conditional_group(positive,wallet,portage://net-p2p/bitcoind-25.1,[any_of_group([package_dependency(portage://net-p2p/bitcoind-25.1,run,no,net-p2p,bitcoin-core,tilde,[25.1,,,25.1],[],[use(enable(berkdb),none)]),package_dependency(portage://net-p2p/bitcoind-25.1,run,no,net-p2p,bitcoin-core,tilde,[25.1,,,25.1],[],[use(enable(sqlite),none)])])]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(package_dependency(portage://net-p2p/bitcoind-25.1,run,no,net-p2p,bitcoin-core,tilde,[25.1,,,25.1],[],[use(enable(daemon),none),use(equal(asm),none),use(equal(berkdb),none),use(equal(examples),none),use(equal(external-signer),none),use(equal(nat-pmp),none),use(equal(sqlite),none),use(equal(systemtap),none),use(equal(test),none),use(equal(upnp),none),use(equal(zeromq),none)]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

}
}

# **************
# The candidates
# **************

subgraph cluster_choices {
rank=same;
color=gray;
label=<<i>candidates</i>>;

}

}
