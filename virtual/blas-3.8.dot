digraph prolog {

# *************
# Graph options
# *************

newrank=true;
concentrate=true;
compound=true;
graph [rankdir=LR,fontname=Helvetica,fontsize=10,ranksep=1.5];#, ranksep=2.5, nodesep=0.2];
edge  [arrowhead=vee];
node  [fontname=Helvetica,fontsize=10];

# **********
# The ebuild
# **********

subgraph cluster_leftcol {
color=gray;
label=<<i>ebuild</i>>;
id [label="portage://virtual/blas-3.8", color=red, width=4, href="../virtual/blas-3.8.svg"];
}

# ****************
# The dependencies
# ****************

subgraph cluster_midcol {
color=gray;
label=<<i>dependencies</i>>;
subgraph cluster_compile {
fillcolor="#eeeeee";
style=filled;
label=<<i>compile</i>>;
# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(use_conditional_group(positive,eselect-ldso,portage://virtual/blas-3.8,[any_of_group([package_dependency(portage://virtual/blas-3.8,install,no,sci-libs,lapack,greaterequal,[3.8,,,3.8],[],[use(enable(eselect-ldso),none)]),package_dependency(portage://virtual/blas-3.8,install,no,sci-libs,openblas,none,[,,],[],[use(enable(eselect-ldso),none)]),package_dependency(portage://virtual/blas-3.8,install,no,sci-libs,blis,none,[,,],[],[use(enable(eselect-ldso),none)])])]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(package_dependency(portage://virtual/blas-3.8,install,no,sci-libs,lapack,greaterequal,[3.8,,,3.8],[],[use(optenable(eselect-ldso),none)]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

}
subgraph cluster_compileandrun {
fillcolor="#eeeeee";
style=filled;
label=<<i>compile and run</i>>;
}
subgraph cluster_run {
fillcolor="#eeeeee";
style=filled;
label=<<i>run</i>>;
# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(use_conditional_group(positive,eselect-ldso,portage://virtual/blas-3.8,[any_of_group([package_dependency(portage://virtual/blas-3.8,run,no,sci-libs,lapack,greaterequal,[3.8,,,3.8],[],[use(enable(eselect-ldso),none)]),package_dependency(portage://virtual/blas-3.8,run,no,sci-libs,openblas,none,[,,],[],[use(enable(eselect-ldso),none)]),package_dependency(portage://virtual/blas-3.8,run,no,sci-libs,blis,none,[,,],[],[use(enable(eselect-ldso),none)])])]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(package_dependency(portage://virtual/blas-3.8,run,no,sci-libs,lapack,greaterequal,[3.8,,,3.8],[],[use(optenable(eselect-ldso),none)]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

}
}

# **************
# The candidates
# **************

subgraph cluster_choices {
rank=same;
color=gray;
label=<<i>candidates</i>>;

}

}
