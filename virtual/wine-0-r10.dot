digraph prolog {

# *************
# Graph options
# *************

newrank=true;
concentrate=true;
compound=true;
graph [rankdir=LR,fontname=Helvetica,fontsize=10,ranksep=1.5];#, ranksep=2.5, nodesep=0.2];
edge  [arrowhead=vee];
node  [fontname=Helvetica,fontsize=10];

# **********
# The ebuild
# **********

subgraph cluster_leftcol {
color=gray;
label=<<i>ebuild</i>>;
id [label="portage://virtual/wine-0-r10", color=red, width=4, href="../virtual/wine-0-r10.svg"];
}

# ****************
# The dependencies
# ****************

subgraph cluster_midcol {
color=gray;
label=<<i>dependencies</i>>;
subgraph cluster_compile {
fillcolor="#eeeeee";
style=filled;
label=<<i>compile</i>>;
}
subgraph cluster_compileandrun {
fillcolor="#eeeeee";
style=filled;
label=<<i>compile and run</i>>;
}
subgraph cluster_run {
fillcolor="#eeeeee";
style=filled;
label=<<i>run</i>>;
# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(any_of_group([package_dependency(portage://virtual/wine-0-r10,run,no,app-emulation,wine-vanilla,none,[,,],[],[use(equal(abi_x86_32),none),use(equal(abi_x86_64),none)]),package_dependency(portage://virtual/wine-0-r10,run,no,app-emulation,wine-proton,none,[,,],[],[use(equal(abi_x86_32),none),use(equal(abi_x86_64),none)]),package_dependency(portage://virtual/wine-0-r10,run,no,app-emulation,wine-staging,none,[,,],[],[use(equal(abi_x86_32),none),use(equal(abi_x86_64),none)])]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(use_conditional_group(positive,proton,portage://virtual/wine-0-r10,[package_dependency(portage://virtual/wine-0-r10,run,no,app-emulation,wine-proton,none,[,,],[],[])]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(use_conditional_group(positive,staging,portage://virtual/wine-0-r10,[package_dependency(portage://virtual/wine-0-r10,run,no,app-emulation,wine-staging,none,[,,],[],[use(enable(staging),positive)])]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

}
}

# **************
# The candidates
# **************

subgraph cluster_choices {
rank=same;
color=gray;
label=<<i>candidates</i>>;

}

}
