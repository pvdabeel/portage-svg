digraph prolog {

# *************
# Graph options
# *************

newrank=true;
concentrate=true;
compound=true;
graph [rankdir=LR,fontname=Helvetica,fontsize=10,ranksep=1.5];#, ranksep=2.5, nodesep=0.2];
edge  [arrowhead=vee];
node  [fontname=Helvetica,fontsize=10];

# **********
# The ebuild
# **********

subgraph cluster_leftcol {
color=gray;
label=<<i>ebuild</i>>;
id [label="portage://virtual/mysql-5.6-r13", color=red, width=4, href="../virtual/mysql-5.6-r13.svg"];
}

# ****************
# The dependencies
# ****************

subgraph cluster_midcol {
color=gray;
label=<<i>dependencies</i>>;
subgraph cluster_compile {
fillcolor="#eeeeee";
style=filled;
label=<<i>compile</i>>;
}
subgraph cluster_compileandrun {
fillcolor="#eeeeee";
style=filled;
label=<<i>compile and run</i>>;
}
subgraph cluster_run {
fillcolor="#eeeeee";
style=filled;
label=<<i>run</i>>;
# *** BEGIN UNKNOWN DEPENDENCY TYPE (TODO) ***
# id -> equal(any_of_group([package_dependency(portage://virtual/mysql-5.6-r13,run,no,dev-db,mariadb,greaterequal,[10.0,,,10.0],[],[use(optenable(embedded),negative),use(optenable(server),none),use(optenable(static),none)]),package_dependency(portage://virtual/mysql-5.6-r13,run,no,dev-db,mysql,greaterequal,[5.6,,,5.6],[],[use(optenable(embedded),negative),use(optenable(server),none),use(optenable(static),negative)]),package_dependency(portage://virtual/mysql-5.6-r13,run,no,dev-db,percona-server,greaterequal,[5.6,,,5.6],[],[use(optenable(embedded),negative),use(optenable(server),none),use(optenable(static),negative)]),package_dependency(portage://virtual/mysql-5.6-r13,run,no,dev-db,mysql-cluster,greaterequal,[7.3,,,7.3],[],[use(optenable(embedded),negative),use(optenable(server),none),use(optenable(static),negative)])]))
# *** END UNKNOWN DEPENDENCY TYPE (TODO) ***

}
}

# **************
# The candidates
# **************

subgraph cluster_choices {
rank=same;
color=gray;
label=<<i>candidates</i>>;

}

}
